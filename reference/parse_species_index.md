# Parse species links from an index page

Parse species links from an index page

## Usage

``` r
parse_species_index(doc)
```

## Arguments

- doc:

  `xml_document` from a `/species` index page.

## Value

Tibble with `species`, `slug`, and `url`.
