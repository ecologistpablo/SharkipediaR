% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sp_search.R
\name{sp_search}
\alias{sp_search}
\title{Search Sharkipedia species names}
\usage{
sp_search(query, index = NULL, all_pages = FALSE, cache = TRUE)
}
\arguments{
\item{query}{Character vector of search terms (case-insensitive).}

\item{index}{Optional species index tibble from \code{\link[=sp_species_urls]{sp_species_urls()}}. For
comprehensive matching, build the index once with
\code{sp_species_urls(all_pages = TRUE)} and pass it here.}

\item{all_pages}{If \code{index} is \code{NULL}, whether to scan all index pages.}

\item{cache}{Passed to \code{\link[=sp_species_urls]{sp_species_urls()}} when \code{index} is \code{NULL}.}
}
\value{
A tibble of matching species with \code{species}, \code{slug}, and \code{url}.
}
\description{
Matches partial scientific names against the species index. By default this
uses a cached index request; set \code{index} to a pre-built index from
\code{\link[=sp_species_urls]{sp_species_urls()}} to avoid repeated downloads.
}
\examples{
\dontrun{
sp_search("Carcharhinus")
}
}
